修复 `backend/app/services/chat_service.py`：ChatService 类的所有方法（create_conversation、get_conversation、list_messages、stream_answer、_persist_user_message、_persist_assistant_message）目前写在模块层级、没有缩进进类体，导致 `ChatService` 没有 create_conversation 属性而报 AttributeError。

步骤：
1. 将上述全部方法缩进移入 ChatService 类体内。
2. 把散落在文件中部的模块级 import 移到文件顶部。
3. `_serialize_citation` 保持为模块级辅助函数。
4. 运行 python 导入检查确认方法已挂到类上。