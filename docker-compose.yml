services:
  postgres:
    # 直接使用 pgvector/pgvector 镜像， CREATE EXTENSION vector 时无需替换镜像
    # 自定义镜像：在 pgvector/pgvector:pg16 之上叠加 zhparser，
    # 一次构建同时支持 pgvector + 中文全文检索（第 6 章）。
    build:
      context: .
      dockerfile: postgres.Dockerfile
    image: rag-kb-postgres:pg16
    container_name: rag-kb-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-rag}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rag}
      POSTGRES_DB: ${POSTGRES_DB:-rag_kb}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - rag_kb_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rag} -d ${POSTGRES_DB:-rag_kb}" ]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  rag_kb_pgdata: